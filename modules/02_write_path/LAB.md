# Module 02 — Write Problems

1. Run the naive path and observe the corruption:
   ```bash
   python modules/02_write_path/naive.py
   ```
2. Read the printed obligation table and final briefing. Note the duplicate
   row and the overwritten briefing.
3. Open `modules/02_write_path/your_fix.py`. Edit `guarded_write` — and only
   this file.
4. Run the test:
   ```bash
   pytest modules/02_write_path/test_write.py
   ```
5. Iterate until it's green.
